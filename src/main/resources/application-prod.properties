# PROD PROFILE
# ==========================================
# Database: Real MySQL (Simulated URL)
spring.datasource.url=jdbc:mysql://prod-db-cluster:3306/users_db
spring.h2.console.enabled=false

# Logging
logging.level.org.springframework.web=INFO

# Custom Business Config
app.environment.name=PRODUCTION SYSTEM
app.feature.beta-enabled=false
